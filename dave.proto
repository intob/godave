syntax = "proto3";

option go_package = "./davepb";

message Msg {
  Op op = 1; // operation
  repeated string addrs = 2; // set of addr:port
  optional bytes val = 3; // anything
  optional bytes time = 4; // big-endian unix milli
  optional bytes nonce = 5; // 32B random
  optional bytes key = 6; // 32B sha256
}

enum Op {
  SETDAT = 0;
  GETDAT = 1;
  DAT = 2;
  GETADDR = 3;
  ADDR = 4;
}

