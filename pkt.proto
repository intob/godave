syntax = "proto3";

option go_package = "./pkt";

message Msg {
  Op op = 1;
  optional Kv kv = 6;
  repeated string route = 3; // set of addr:port
}

message Kv {
  string key = 1;
  bytes val = 2;
  int64 t = 3;
}

enum Op {
  SET = 0;
  GET = 1;
  VAL = 2;
}

