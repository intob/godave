syntax = "proto3";

option go_package = "./pkt";

message Msg {
  Op op = 1;
  repeated string route = 3; // each is addr:port
  string key = 4;
  bytes value = 5;
}

enum Op {
  SET = 0; // SET value of key
  GET = 1; // GET value of key
  VAL = 2; // response to GET
}

